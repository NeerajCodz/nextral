{
  "name": "nextral",
  "version": "0.1.0",
  "description": "Node package for the Nextral production memory runtime.",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "napi build --platform",
    "build:debug": "napi build --platform --debug"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "typescript": "^5.6.3"
  }
}
