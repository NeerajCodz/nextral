name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  rust:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Run clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Run tests
        run: cargo test --workspace

  node:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Install root dependencies
        run: npm ci
      - name: Install node binding dependencies
        working-directory: bindings/node
        run: npm ci
      - name: Build node package
        run: npm --workspace bindings/node run build
      - name: Node smoke
        run: node -e "const n=require('./bindings/node/index.js'); console.log(n.e2ESmoke())"

  python:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install maturin
        run: python -m pip install maturin
      - name: Build Python wheel
        working-directory: bindings/python
        run: python -m maturin build
      - name: Install Python wheel (Linux)
        if: runner.os == 'Linux'
        working-directory: bindings/python
        run: python -m pip install $(ls target/wheels/*.whl | head -1)
      - name: Install Python wheel (Windows)
        if: runner.os == 'Windows'
        working-directory: bindings/python
        run: python -m pip install (Get-ChildItem target/wheels/*.whl | Select-Object -First 1).FullName
      - name: Python CLI help
        working-directory: bindings/python
        run: python -m nextral --help

  docker-e2e:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Install node binding dependencies
        working-directory: bindings/node
        run: npm ci
      - name: Build node package
        run: npm --workspace bindings/node run build
      - name: Run Docker integration E2E
        run: bash scripts/e2e-docker.sh
