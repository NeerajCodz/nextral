{
  "name": "nextral",
  "version": "0.0.1",
  "description": "Info-only npm package for Nextral publishing metadata.",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "napi build --platform",
    "build:debug": "napi build --platform --debug"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "typescript": "^5.6.3"
  }
}
